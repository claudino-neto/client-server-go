syntax = "proto3";

package impl;

option go_package = "gRPC/proto;impl";

service HTTPService {
    rpc GET (Request) returns (Response);
}

message Request {
    string link = 1;
}

message Response {
    string body = 1;
}
